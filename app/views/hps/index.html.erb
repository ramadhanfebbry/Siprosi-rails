<div class="card">
  <div class="header">
    <h4 class="title">Hasil Produksi</h4>
    <p class="category">Here is a subtitle for this table</p>
  </div>
  <div class="content table-responsive table-full-width">
    <table class="table table-striped">
      <thead>
        <th>ID</th>
        <th>Instruksi Produksi</th>
        <th>Rencana Produksi</th>
        <th>Hasil Produksi</th>
        <th>Target Produksi</th>        
        <th>Aksi</th>
      </thead>
      <tbody>
        <% @hps.each_with_index do |hp| %>
        <tr>
          <td><%= hp.id %></td>   
          <td><%= hp.ip.id %></td>   
          <td><%= hp.ip.pb.rp.id %></td>   
          <td><%= hp.hasil_produksi %></td>
          <td><%= hp.target_produksi %></td>        
          <td>
            <%= link_to 'Detail', hp_path(hp), class: "btn btn-info btn-fill btn-sm" %>            
          </td>
        </tr>              
        <%end%>
      </tbody>
    </table>

  </div>
</div>
